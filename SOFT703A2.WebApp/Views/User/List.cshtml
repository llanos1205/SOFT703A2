@model SOFT703A2.Infrastructure.Contracts.ViewModels.User.IListUserViewModel

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>Users</h2>
<a href="@Url.Action("Add", "User")" class="btn btn-primary">Create User</a>
<table class="table">
    <thead>
    <tr>
        <th>Username</th>
        <th>Email</th>
        <th>Phone</th>
        <th>First Name</th>
        <th>Last Name</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var users in Model.Users)
    {
        <tr>
            <td>@users.UserName</td>
            <td>@users.Email</td>
            <td>@users.PhoneNumber</td>
            <td>@users.FirstName</td>
            <td>@users.LastName</td>
            <td>
                <a href="@Url.Action("Detail", "User", new { id = users.Id })" class="btn btn-primary">Details</a>
                <a href="@Url.Action("Delete", "User", new { id = users.Id })" class="btn btn-danger">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>